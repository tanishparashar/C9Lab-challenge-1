# role-auth-frontend (React + Vite)

## Setup
```bash
npm install
cp .env.example .env
npm run dev
```

Backend should be running on: `http://127.0.0.1:8000`

You can change backend url in `.env` using `VITE_API_BASE_URL`.
